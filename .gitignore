# Local state (contains no secrets but is environment state, not source).
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config / credentials
.terraformrc
terraform.rc

# Note: .terraform.lock.hcl and terraform.tfvars ARE committed.
